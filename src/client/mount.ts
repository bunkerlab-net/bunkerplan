/**
 * The two ids that tie the server render to the client hydration. Shared so
 * neither side can rename one without the other.
 */
export const ROOT_ID = "app";
export const PAGE_PROPS_ID = "page-props";
